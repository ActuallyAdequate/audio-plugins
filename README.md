# Border Security Plugin

## Building

After installing [Rust](https://rustup.rs/), you can compile Border Security Plugin as follows:

```shell
cargo xtask bundle border_security_plugin --release
```
